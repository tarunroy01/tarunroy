{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "npm --prefix portfolio run dev",
    "build": "npm --prefix portfolio run build",
    "deploy": "npm --prefix portfolio run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
